#pragma once

// GENERATED FILE -- do not edit. Produced by configure_file from
// m10_embedded_game_manifests.hpp.in, with each constant's body read verbatim
// from the canonical on-disk game manifest in src/manifests/irem_m10/games at
// configure time. CMAKE_CONFIGURE_DEPENDS re-runs configuration whenever a
// source .toml changes, so the embedded copy cannot drift from the file.
//
// Access these through game_manifest_toml() in m10_game_manifests.hpp.

#include <array>
#include <string_view>
#include <utility>

namespace mnemos::manifests::irem_m10::embedded {

@MNEMOS_IREM_M10_EMBEDDED_CONSTANTS@
    inline constexpr std::array<std::pair<std::string_view, std::string_view>,
                                @MNEMOS_IREM_M10_GAME_MANIFEST_COUNT@>
        game_manifests{{
@MNEMOS_IREM_M10_EMBEDDED_ENTRIES@
        }};

} // namespace mnemos::manifests::irem_m10::embedded
