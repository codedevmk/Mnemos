#include "redalert_game_manifests.hpp"

namespace mnemos::manifests::irem_redalert {
    static_assert(main_rom_size == 0x10000U);
    static_assert(audio_rom_size == 0x10000U);
    static_assert(proms_size == 0x0200U);
} // namespace mnemos::manifests::irem_redalert
