# Amiga Models

Model descriptors belong here. A model descriptor should say which chipset
profile, base RAM, expansion slots, and default devices a machine has; it
should not duplicate the shared machine implementation.

`amiga_models.*` is the current descriptor table for the public A500, A500+,
A600, and A2000 profiles.
