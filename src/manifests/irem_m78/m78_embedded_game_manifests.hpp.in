#pragma once

// GENERATED FILE -- do not edit. Produced by configure_file from
// m78_embedded_game_manifests.hpp.in, with each constant's body read verbatim
// from the canonical on-disk game manifest in src/manifests/irem_m78/games at
// configure time. CMAKE_CONFIGURE_DEPENDS re-runs configuration whenever a
// source .toml changes, so the embedded copy cannot drift from the file.
//
// Access these through game_manifest_toml() in m78_game_manifests.hpp.

#include <array>
#include <string_view>
#include <utility>

namespace mnemos::manifests::irem_m78::embedded {

@MNEMOS_IREM_M78_EMBEDDED_CONSTANTS@
    inline constexpr std::array<std::pair<std::string_view, std::string_view>,
                                @MNEMOS_IREM_M78_GAME_MANIFEST_COUNT@>
        game_manifests{{
@MNEMOS_IREM_M78_EMBEDDED_ENTRIES@
        }};

} // namespace mnemos::manifests::irem_m78::embedded
