name: ci

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  format:
    name: clang-format
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install clang-format
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format

      - name: Check formatting
        run: |
          git ls-files '*.c' '*.cc' '*.cpp' '*.cxx' '*.h' '*.hh' '*.hpp' '*.hxx' \
            | xargs --no-run-if-empty clang-format --dry-run --Werror

  build-test:
    name: ${{ matrix.preset }}
    needs: format
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            preset: windows-msvc-debug
          - os: windows-latest
            preset: windows-msvc-release
          - os: ubuntu-24.04
            preset: linux-gcc-debug
          - os: ubuntu-24.04
            preset: linux-gcc-release
          - os: ubuntu-24.04
            preset: linux-clang-debug
          - os: ubuntu-24.04
            preset: linux-clang-release

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux tools
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build

      - name: Configure, build, and test on Windows
        if: runner.os == 'Windows'
        shell: cmd
        env:
          PRESET: ${{ matrix.preset }}
        run: |
          for /f "usebackq tokens=*" %%I in (`"%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`) do set VSINSTALL=%%I
          if not defined VSINSTALL exit /b 1
          set VSDEVCMD=%VSINSTALL%\Common7\Tools\VsDevCmd.bat
          if not exist "%VSDEVCMD%" exit /b 1
          call "%VSDEVCMD%" -arch=x64
          cmake --preset %PRESET%
          cmake --build --preset %PRESET%
          ctest --preset %PRESET% --output-on-failure

      - name: Configure
        if: runner.os == 'Linux'
        run: cmake --preset ${{ matrix.preset }}

      - name: Build
        if: runner.os == 'Linux'
        run: cmake --build --preset ${{ matrix.preset }}

      - name: Test
        if: runner.os == 'Linux'
        run: ctest --preset ${{ matrix.preset }} --output-on-failure

  foundation-coverage:
    name: foundation-coverage
    needs: format
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install coverage tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang llvm ninja-build

      - name: Configure
        run: cmake --preset linux-clang-coverage

      - name: Build
        run: cmake --build --preset linux-clang-coverage

      - name: Measure Foundation branch coverage
        shell: pwsh
        run: |
          ./tools/coverage/measure-foundation-branch-coverage.ps1 `
            -BuildDir build/linux-clang-coverage `
            -MinimumBranchCoverage 90.0
