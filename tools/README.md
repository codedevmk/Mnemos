# Tools

Build helpers, code generators, validators, ROM hashers, and maintenance scripts
live here.

`governance/` holds the gate scripts from `constitution/MNE-CTX-PLAN-001.md`
§8 (ADR lint, license audit, naming validator, token budgets), run by the CI
`governance` job.
