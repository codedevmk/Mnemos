# Plan: Irem M72 subsystem (staged port)

> **Status (2026-06-23):** Phases A (incl. the A2 0F extension group with
> hermetic INS/EXT bitfield regressions and FPO2/BRKEM operand-consumption
> stubs), B, C,
> and D are complete and merged, plus phase-E infrastructure: DIP override +
> TATE orientation frontend plumbing, the MCS-51 core (`chips/cpu/mcs51`), the
> optional protection-MCU board wiring (latch pair, shared RAM, MOVX sample
> window activated by an `mcu` ROM-set region, including both DPTR and
> P2-latched `@R0` / `@R1` MOVX forms, 8051 port read-modify-write latch
> semantics, IE/IP two-level interrupt
> priority/nesting with post-RETI and IE/IP-access interrupt deferral, serial RI/TI interrupt arbitration, frame-level SBUF
> transmit/receive timing, timer modes
> 0/1/2/3 plus TMOD GATE and external counter pins, and mechanical all-opcode
> operand-consumption coverage for real i8751 programs),
> ROM-backed `soundcpu` Z80 maps,
> including public 64 KiB sound declarations with the top 4 KiB shadowed by
> Z80 RAM, true-M72 work-RAM board-parameter families, embedded checked-in game
> manifest resolution for standard set zips/directories, declarative
> clone-parent zip/directory fallback for shared ROM dumps with media issues
> when parent resolution fails,
> set-specific DIP defaults for the true-M72
> board profiles, manifest-level DIP option parsing with checked-in true-M72
> family DIP tables including conditional Irem coinage and clone parent
> inheritance, manifest-layer whole-board save/load plus a player-adapter
> runtime save target whose board component tracks the media-fingerprinted
> board-state schema and whose adapter component captures frame count,
> audio-drain cursor, DAC mix continuity, and frontend input snapshots; the
> player session capability advertises frame-exact rollback readiness for
> chip state, mid-frame video framebuffer, RAM, protection latches, sample cursors,
> DAC write timing, input/DIP/control latches including cabinet service/test inputs,
> control-register coin-counter outputs plus flip-screen rendering/state, the
> CPU-visible sprite-DMA-complete input bit plus full latched sprite-entry
> traversal across the board's sprite RAM,
> scanline-composed video so raster-time scroll changes affect the visible
> lines after the write,
> full-board debug chip enumeration, protection
> shared-RAM memory-view exposure, explicit frontend `service` / `test` mapping
> with legacy `mode` service aliasing plus v1/v2 adapter input-state loading,
> and checked-in ROM-set manifests for `rtype`, `bchopper`,
> `mrheli`, `nspirit`, `nspiritj`, `loht`, `lohtj`, `imgfight`,
> `airduelm72`, plus parent-inherited clone manifests for `rtypej`,
> `rtypejp`, `rtypeu`, `rtypeb`, `imgfightj`, `imgfightjb`, `lohtb2`,
> `lohtb3`, `airdueljm72`, plus standalone true-M72 manifests for
> `xmultiplm72`, `dbreedjm72`, and `gallopm72`, and explicit no-dump MCU
> HLE manifests for `dbreedm72` and `dkgensanm72` whose `[[hle]]` profiles map
> the startup protection RAM inversion surface, acknowledge the V30 command
> latch, use manifest-declared profile-bounded sample-trigger cursor starts, and
> expose the profile-specific service-ROM checksum response bytes without declaring
> fake MCU dumps or copying protection code; unprotected boards now leave the
> absent MCU latch port as open bus; unsupported MCU HLE profiles and
> no-dump HLE profiles missing sample-trigger metadata, pointing outside the
> loaded `samples` region, or depending on missing sample dump files now surface as loader issues instead of silently
> booting as unprotected or partially-HLE boards; declarative protected sets
> with missing MCU dump files report the ROM-set issue but disable MCU execution
> instead of scheduling a filled fake i8751, and
> non-M72 zip-local board declarations now surface as loader issues instead of
> silently booting as empty M72 boards,
> and the Irem M72 player media descriptor publishes a CRC32 over the resolved
> set metadata plus loaded resident ROM regions so capability discovery can
> identify the exact parent-fallback-resolved image instead of reporting
> degraded ROM-set media; missing, unreadable, unsafe, undeclared, or
> manifest-mismatched clone parents are carried as ROM-set validation issues
> instead of stderr-only warnings, and a mismatched parent zip is not allowed
> to supply shared dumps.
> Current validation is synthetic plus manifest
> parser/region-contract coverage and data-gated hooks:
> `MNEMOS_M72_RTYPE_SET` for the first-game path and `MNEMOS_M72_PROTECTED_SET`
> for protected M72 sets with either a dumped MCU or an explicit no-dump HLE
> profile, plus `MNEMOS_M72_VERTICAL_SET` for vertical true-M72 orientation/data
> validation, and `MNEMOS_M72_SET_DIR` for an all-roster checked-in true-M72
> matrix that accepts a platform path-list of roots containing `<set>.zip`,
> `<set>\`, or single-inner-set wrapper zips per embedded manifest and validates
> parent fallback, CRC-clean loads, board wiring, orientation, sound release,
> and a non-blank frame. `scripts/irem_m72/run-corpus-smoke.ps1` drives the
> same data-gated artifacts through `mnemos_player` save/load/screenshot smoke
> proof; those hooks are now first-class G6 oracle targets
> (`mnemos_apps_player_irem_m72_rtype_golden_test`,
> `mnemos_apps_player_irem_m72_protected_golden_test`, and
> `mnemos_apps_player_irem_m72_vertical_golden_test`, plus the roster matrix
> `mnemos_apps_player_irem_m72_roster_golden_test`) instead of hidden inside the
> mixed adapter unit target, whose CTest entry excludes `[data]` cases.
> 2026-06-25 local artifact pass: mixed `D:\emu\irem` inventory now separates
> root archives from `m72`, `M81`, `m82`, `M84`, `M15`, and `i8751` folders
> instead of treating the tree as a pure M72 root. The corpus runner accepts
> multiple `-RomDir` values and treats sorted non-M72 roots (`M81`, `m82`,
> `M84`) as a clean zero-candidate result instead of failing or admitting them
> as M72 coverage, while `D:\emu\irem\m72 -MaxSets 2` still proves the positive
> roster path. `scripts/irem/inventory-corpus.ps1 -Root D:\emu\irem -Recurse`
> records the mixed local corpus as metadata only: 121 items across `root`,
> `m72`, `M15`, `M81`, `m82`, `M84`, `M107`, and `i8751` after ignoring
> archive-only container folders as unpacked sets, with 1 item matching the
> checked-in M15 ROM contract, 37 items matching checked-in playable M72
> manifests, 5 items matching checked-in M81 ROM contracts, 5 items matching
> checked-in M82 R-Type II manifests, 2 items matching checked-in M84 ROM
> contracts, and 8 items matching checked-in M107 ROM contracts. Inventory now
> distinguishes manifest tracking from player loadability: 58 local items match
> checked-in Irem manifests, 52 are loadable through ZIP / single-inner-ZIP /
> folder routes, and the six tracked `.7z` items remain metadata-only until
> converted or unpacked. No sorted top-level board bucket is completely
> untracked anymore; `board_family_candidates` now only keeps 19 untracked /
> misbucketed `m72` folder items explicit instead of counting them as true-M72
> support. M15 now has a ROM-contract-only checked-in manifest in
> `src/manifests/irem_m15` for `headoni`; `MNEMOS_M15_SET_DIR=D:\emu\irem\M15`
> unwraps the local wrapper ZIP and proves it loads CRC-clean, including the
> directory-prefixed entry aliases used by the local nested ZIP. M81 now has
> first-pass executable board/profile coverage in `src/manifests/irem_m81` for
> `dbreed`, `hharry`, and `xmultipl`: `MNEMOS_M81_SET_DIR=D:\emu\irem\M81`
> unwraps the local wrapper ZIPs and proves those sorted artifacts load
> CRC-clean, hermetic tests run a V30/Z80/YM2151/DAC/8259 frame, and save-state
> identity rejects mismatched M81 board-layout profiles. Duplicate / misbucketed
> `dbreed` copies outside `M81` are also visible in the inventory. M84 now has
> ROM-contract-only checked-in manifests in `src/manifests/irem_m84` for
> `hharryb` and `hharryu`; `MNEMOS_M84_SET_DIR=D:\emu\irem\M84;D:\emu\irem\M81`
> unwraps the local split wrapper ZIPs and proves they load CRC-clean when
> composed with the M81 `hharry` parent. M107 has
> ROM-contract-only checked-in manifests in `src/manifests/irem_m107` for
> `airass` and `firebarr`; `MNEMOS_M107_SET_DIR=D:\emu\irem\M107` proves the
> sorted local ZIPs load CRC-clean, while root and `m72`-folder duplicate
> artifacts are visible as M107 and the Fire Barrel `.7z` copies remain
> metadata-only until converted or unpacked. M82 now
> has a first-pass executable player adapter routed through
> `--system irem_m82`, not the M72 board profile.
> `MNEMOS_M82_SET_DIR=D:\emu\irem` unwraps the local R-Type II wrapper ZIPs
> and proves `rtype2`, `rtype2j`, `rtype2jc`, and `rtype2m82b` load CRC-clean
> through the embedded M82 manifests, clone-parent fallback, and player smoke.
> The M82 video path now has a RAM-backed first-pass tile/sprite/palette
> renderer plus scanline-paced IR0 vblank and IR2 raster-compare delivery
> through the 8259, with a diagnostic fallback for uninitialized development
> launches, but not final raster phase / palette-banking / DIP parity. M81 is
> now player-routable through `--system irem_m81`; `MNEMOS_M81_SET_DIR` proves
> all three local wrapper ZIPs through the adapter, resident media validation,
> rollback-ready save-state, and nonblank player frames. M15, M84, and M107
> now have executable board/profile layers but still need final authentic
> video/priority/sound/exact-raster/screenshot parity beyond their first-pass
> boards; M81 still needs final authentic video/raster/DIP parity. Dedicated local M72
> artifact gates pass with
> `MNEMOS_M72_RTYPE_SET=D:\emu\irem\R-Type_Arcade_EN.zip`,
> `MNEMOS_M72_PROTECTED_SET=D:\emu\irem\imgfight.zip`,
> `MNEMOS_M72_PROTECTED_MCU_SET=D:\emu\irem\M72\nspirit.zip`, and
> `MNEMOS_M72_VERTICAL_SET=D:\emu\irem\imgfight.zip`. Recursive discovery plus
> the longer R-Type 600/1200-frame shard proves clean player save/load/screenshot
> smoke for all 23 checked-in true-M72 sets: `airdueljm72`, `airduelm72`,
> `bchopper`, `dbreedjm72`, `dbreedm72`, `dkgensanm72`, `gallopm72`,
> `imgfight`, `imgfightj`, `imgfightjb`, `loht`, `lohtb2`, `lohtb3`,
> `lohtj`, `mrheli`, `nspirit`, `nspiritj`, `rtype`, `rtypeb`, `rtypej`,
> `rtypejp`, `rtypeu`, and `xmultiplm72` across standard folders,
> unpacked directories, wrapper ZIPs, split clone/parent ZIP pairs, and generated
> per-set subsets from collection ZIPs. The corpus runner's default fallback
> probes include 900 frames because `rtypeb` stays black at 300/600 frames but
> reaches a lit post-load frame at 900. The corpus runner now requires both a
> lit post-load screenshot and zero media-validation issues, so incomplete
> protected sets are not counted as pass-throughs. The current
> `D:\emu\irem\M72\nspirit.zip` is CRC-complete for `nspirit` and `nspiritj`,
> while the unpacked `D:\emu\irem\M72\nspirit` folder is a stale incomplete
> source. Updated exact scans now report `44/44` present for
> `gallopm72` + `nspirit`, `20/20` for `lohtj` with parent `loht.zip`,
> `30/30` for `lohtb2` with parent `loht.zip`, and `94/94` for the previous
> `gallopm72` / `nspirit` / `lohtj` / `lohtb2` blocker group. A full
> `scripts/irem_m72/find-missing-artifacts.ps1 -Root D:\emu\irem -Recurse`
> preflight now reports `417/417` checked-in M72 manifest artifacts present.
> The `lohtj` / `lohtb2` manifests now alias the local `loht.zip` parent/shared
> filenames, and targeted `gallopm72` / `lohtj` / `lohtb2` corpus smoke passes
> `3/3`.
> The optional one-command roster CTest with `MNEMOS_M72_SET_DIR=D:\emu\irem`
> now passes as current full-roster player proof after the source-ranking,
> supplemental-media composition, held-MCU-latch, and late Air Duel audio-window
> fixes. Current evidence: `mnemos_apps_player_irem_m72_roster_golden_test`
> passes with its default 900-frame roster window, validating CRC-clean loads,
> parent/supplemental media, board wiring, orientation, sound release, and
> non-blank frames for every checked-in true-M72 manifest in the local corpus.
> The artifact scanner now has a
> `-MissingFromReport <json>` mode for focused follow-up searches from the
> current blocker list, tolerates malformed ZIPs, and uses size-aware `.7z`
> listing to avoid extracting unrelated archive members. Older missing-only
> scans are superseded by the updated local ZIP set.
> `scripts\irem\run-local-corpus.ps1` now wires the current local `D:\emu\irem`
> layout into the available-artifact data-gated Irem proof, including M90/M92
> wrapper ZIPs stored in the mixed root. It deliberately omits
> `MNEMOS_M72_SET_DIR` unless `-IncludeFullM72Roster` is passed, so the strict
> roster golden should be rerun before quoting a current full-roster player
> result.
> Remaining items are gated on stronger authenticity evidence:
> the 8088/V20 corpus (`MNEMOS_V30_TESTS_DIR`),
> complete per-game MCU/protection entry behavior
> including the `dbreedm72` / `dkgensanm72` no-dump HLE profiles, manual-backed
> corrections for any MAME-assumed DIP locations, and then phase F (M92), which
> is gated on M72 parity by design.

**Date:** 2026-06-10
**ADR:** [0012-irem-arcade-subsystem](../adr/0012-irem-arcade-subsystem.md)
**Pattern:** mirrors the Sega 32X staged port
(`docs/plans/2026-06-09-sega-32x-port.md`) and the m68000 / SH-2 phased CPU
build-outs.

## Goal

A functional Irem M72 board family driven from the player via an `irem_m72`
family id, with R-Type as the first game: NEC V30 main CPU, Z80 sound CPU
(already in tree), the M72 tilemap/sprite video unit, YM2151 + DAC audio, and
declarative per-game TOML manifests layered on a shared board manifest. No
existing console path changes except the additive extensions named in ADR 0012.

## Hard prerequisites (blockers, surfaced up front)

1. **Game ROM sets are never committed.** Board bring-up, boot, and parity
   tests skip (`SKIP_RETURN_CODE 4`) unless an env var points at a locally
   supplied R-Type ROM set. Functional CPU/loader/video work proceeds without
   them against synthetic fixtures.
2. **V30 conformance corpus** (recommended): the public per-instruction JSON
   corpora for the 8088/V20 family (SingleStepTests format, the same shape the
   Z80 singlestep harness consumes). Env-gated via `MNEMOS_V30_TESTS_DIR`,
   never committed.
3. **Reference emulators (MAME et al.) are licence-incompatible for code
   lifting** (GPL vs the Apache-2.0/MIT split, ADR 0003). They serve as
   behavioural references only; implementation is from public ISA / hardware
   documentation, gated by the conformance corpus (ADR 0006 policy).

## Phase A — V30 CPU core (`src/chips/cpu/v30`)

The reusable, system-agnostic piece. Buildable and testable with no board,
no ROMs — only public NEC V30 / 8086-family ISA documentation.

- **A1 (this increment):** chip skeleton + executable core — `icpu` subclass
  with the V30 programming model (AW/BW/CW/DW, SP/BP/IX/IY, the four segment
  registers, PC, PSW — surfaced under the conventional Intel names
  AX..DX/SI/DI/CS..SS/IP/FLAGS that every corpus and reference doc uses),
  little-endian word access composed over `ibus` byte reads, 20-bit physical
  addressing, `reset` (CS=FFFF, IP=0), prefix handling (segment override,
  REP/REPNE + the V-series REPC/REPNC, LOCK), modrm/EA decode, and the 8086
  base instruction map plus the 186/V-series additions M72 code uses (PUSHA/
  POPA, PUSH/IMUL imm, INS/OUTS, shift-by-imm, ENTER/LEAVE, BOUND). INTR/NMI
  dispatch (vector via injected ack callback), separate 16-bit I/O space via
  port callbacks (the Z80 pattern), HLT, save/load state, `introspection_surface`
  (register_view + trace_target), factory registration (`nec.v30`), unit tests
  per instruction group. First-order documented cycle costs; exact V30 timing
  is a later increment.
- **A2:** the V30 `0F`-prefix extension group (bit-field ops, BCD string ops,
  TEST1/SET1/CLR1/NOT1), plus the 8080-emulation-mode entry points stubbed as
  documented no-ops with operand-byte consumption (no M72 title uses them).
- **A3:** timing refinement — per-EA cycle table, V30-specific (not 8086)
  instruction timings, prefetch-queue first-order model if parity work needs it.
- **A4:** conformance harness polish: per-instruction undefined-flag masks,
  cycle-count comparison once A3 lands. The functional regs+memory compare
  harness itself ships in A1 (`MNEMOS_V30_TESTS_DIR`, skips when unset).

## Phase B — ROM-set loader + board manifest (`src/manifests/irem_m72`)

- Multi-file ROM regions in `manifests/common`: per-file offset/stride
  (even/odd interleave for the V30 program pair), per-file CRC32/SHA, zip
  bundle support composing the existing `compression` + `security/cryptography`
  modules. Unit-tested with synthetic fixtures.
- `m72.toml` board manifest: V30 + Z80 on their buses, work RAM, video RAM,
  sprite RAM, palette RAM, sound latch MMIO, I/O ports (inputs/DIPs/coin),
  program ROM banking callback. `games/rtype.toml` as the first game leaf.
- Little-endian wide bus accessors in topology (mirror of the BE ones from the
  SH-2/68000 hot-path work) — small standalone commit ahead of board wiring.
- First visual artifact: tile/sprite ROM regions exposed as introspection
  `debug_layer`s through the existing `--screenshot` path (raw decode viewer).

## Phase C — Video + IRQs (`src/chips/video/irem_m72_video`)

- Tilemap layers (two scrolling planes), sprites, per-tile priority,
  flip-screen, palette with CPU-visible 5-bit low-byte planes and disconnected
  A9 mirrors; 384×256 active raster around 55 Hz. Visible scanlines
  compose at beam-line start, so raster-time register changes affect later
  scanlines without repainting earlier lines.
- Scanline-compare raster IRQ + vblank IRQ into the V30 via the board's IRQ
  callback wiring; sprite DMA trigger.
- R-Type attract mode renders; screenshot-parity baseline established
  (data-gated on the ROM set).

## Phase D — Audio + inputs

- `src/chips/audio/ym2151` (reusable well beyond Irem) + `src/chips/audio/dac`;
  sound latch + Z80 INT/NMI plumbing; sample playback path with DAC writes
  mixed at the YM2151 sample boundary.
- Scheduler rational-divider extension for the YM2151's 3.579545 MHz crystal
  (32 MHz master: V30 = /4, Z80 = /8 stay exact integer dividers).
- Cabinet inputs (coin/start/service/test) + DIP banks: manifest `[input]` /
  `[dip]` schema, peripheral_sdk device, player key mapping. R-Type playable.
  The current ROM-set schema parses `[[dip]]` option tables with optional raw
  condition masks and the Irem adapter retains them; the checked-in true-M72
  roster carries per-family DIP tables, including conditional coinage and clone
  parent inheritance.

## Phase E — Roster + player polish

- Mr. Heli / Image Fight / Air Duel as data-only game manifests. Gated on the
  i8751 MCU question per ADR 0012 (`chips/cpu/mcs51`, or an explicitly marked
  per-game simulation callback).
- Display orientation (vertical/TATE) metadata + rotated presentation in the
  player for the vertical titles.
- No-dump true-M72 protection sets (`dbreedm72`, `dkgensanm72`) declare
  manifest-level `[[hle]]` MCU profiles; current coverage includes the startup
  inverted shared-RAM surface, command-latch acknowledge, and sample-trigger
  cursor setup through manifest-owned profile-bounded trigger tables. Duplicate
  trigger ids are rejected by the ROM-set parser, and a supported no-dump profile
  without a trigger table is reported and disabled instead of activating partial
  HLE. Set-specific protection entry routines still need authentic artifact
  validation.

## Phase F — M92 (own ADR when M72 is at parity)

V33 as a V30 configuration, GA20 PCM (`chips/audio`), GA21/GA22 video
(`chips/video`), sound-CPU opcode decryption.

## Parallel Irem board-family staging

- M15 has a checked-in ROM-contract layer in `src/manifests/irem_m15` for
  `headoni`. It embeds the source TOML at configure time, preserves the early
  6502-era board-family boundary away from V30 M72/M81/M82/M84/M107 boards,
  tests the six 1 KiB program ROM placements, the `e4.9d` reset-vector reload
  at `$fc00`, and directory-prefixed nested-ZIP aliases, and loads the local
  `D:\emu\irem\M15` wrapper ZIP corpus CRC-clean through `MNEMOS_M15_SET_DIR`.
  It now also has an M15-owned executable board/profile using the
  shared 6502-family CPU core in bare-MOS-6502 mode at `733125 Hz`, with the
  M-15 scratch/video/color/chargen RAM windows, `$a000/$a100/$a200/$a300/$a400`
  P2/sound/DIP/P1/control MMIO, frame IRQ vector pulse, coin-triggered NMI,
  active-high Head On P1/P2 controls, `0x11` DIP default, active-low flip
  control, `$a100` sound-latch transition accounting with per-bit rise/fall
  counters plus active-low bit-6 speaker edge tracking, tile/color/chargen frame
  rendering with M-15 tile scan order, scanline-paced frame stepping where
  frame-IRQ writes affect later visible scanlines without repainting earlier
  ones, whole-board save/load identity, and `src/apps/player/adapters/irem_m15`
  routing through `--system irem_m15` / `m15`. The local Head On wrapper ZIP
  passes data-gated adapter smoke plus direct screenshot and save/load player
  smoke. Remaining M15 work is board-evidenced discrete sample mappings/analog
  sound behavior, analog color proof, exact raster phase proof, and authentic
  screenshot parity.

- M81 has checked-in manifests and a first-pass executable board/profile in
  `src/manifests/irem_m81` for `dbreed`, `hharry`, and `xmultipl`. It embeds
  source TOMLs at configure time, preserves the board-family boundary away from
  true M72, tests V30 program boot-chunk reload declarations,
  sound/sample/PROM/graphics regions, loads the local `D:\emu\irem\M81` wrapper
  ZIP corpus CRC-clean through `MNEMOS_M81_SET_DIR`, runs a
  V30/Z80/YM2151/DAC/8259 frame, and includes save-state identity coverage for
  the three M81 board-layout profiles. `src/apps/player/adapters/irem_m81`
  routes `--system irem_m81`, reports resident media validation, exposes
  rollback-ready save-state/capability discovery, and proves all three local
  wrapper ZIPs through data-gated player smoke. Remaining M81 work is final
  authentic video/raster priority, inputs/DIPs, and board timing parity.

- M84 has a checked-in ROM-contract layer in `src/manifests/irem_m84` for
  `hharryb`, `hharryu`, `ltswords`, and `gallop`. It embeds source TOMLs at
  configure time, preserves the board-family boundary away from M81, tests the
  1 MiB V30/V35 program reload declarations plus M84-specific PAL/PLD regions,
  loads the local Hammerin' Harry split wrapper ZIP corpus CRC-clean when
  composed with the M81 `hharry` parent, and loads the standalone local
  `D:\emu\irem\M72\ltswords` folder plus complete
  `D:\emu\irem\M72\gallop.zip` archive through
  `MNEMOS_M84_SET_DIR=D:\emu\irem\M84;D:\emu\irem\M81;D:\emu\irem\M72`. It now
  has an M84-owned executable wrapper that runs those routes through the
  M81-compatible V30/V35, Z80, YM2151, DAC, and KNA91-style board core while
  preserving M84 manifest/save identity, routes `--system irem_m84` / `m84`,
  reports resident media validation, exposes rollback-ready save-state/capability
  discovery, applies Gallop's manifest DIP default (`0xf9bf`), and proves Gallop
  with data-gated player, screenshot, and save/load smoke. Remaining M84 work is
  to replace or verify the compatibility-core assumptions against board evidence
  for M84-specific memory and I/O behavior, Hammerin' Harry/Cosmic Cop/Ken-Go
  video/priority paths, board-authentic DIP behavior, raster timing, and
  authentic screenshot parity.

- M107 has a checked-in ROM-contract layer in `src/manifests/irem_m107` for
  `airass` and `firebarr`. It embeds source TOMLs at configure time, preserves
  the board-family boundary away from M72/M81/M82, tests main-program boot-chunk
  reload declarations, interleaved sound program ROMs, graphics ROM group bounds,
  Air Assault data/sample regions, and loads the local `D:\emu\irem\M107` ZIP
  corpus CRC-clean through `MNEMOS_M107_SET_DIR`. It now also has an M107-owned
  first-pass executable board/profile with separate main and sound V-series CPU
  instances, M107-local RAM/shared/video/sprite/palette windows, YM2151 plus PCM
  command/status surfaces, inputs/DIPs, whole-board save/load identity, and a
  diagnostic video path driven by M107 graphics/subdata/sample regions rather
  than older Irem board wiring. `src/apps/player/adapters/irem_m107` routes
  `--system irem_m107` / `m107`, reports resident media validation, exposes
  rollback-ready save-state/capability discovery, and proves the local M107 ZIP
  corpus plus direct Air Assault screenshot/save-load smoke. Remaining M107 work
  is to replace or verify the first-pass V33 configuration, M107 memory and I/O
  map, GA20/GA21/GA22 video/priority and PCM behavior, sound-CPU details, DIP
  behavior, raster timing, and authentic screenshot parity.

- M82 / R-Type II has a checked-in ROM-contract layer in `src/manifests/irem_m82`
  for `rtype2`, `rtype2j`, `rtype2jc`, and the local nested `rtype2m82b` set
  plus a first-pass executable board/adapter in
  `src/apps/player/adapters/irem_m82`. It embeds source TOMLs at configure time,
  preserves clone parent inheritance, tests main-program reset-vector reload,
  sound/sample/PROM inheritance, graphics region bounds, V30/Z80/YM2151/DAC/8259
  board stepping, scanline-paced IR0 vblank and IR2 raster-compare delivery,
  RAM-backed first-pass tile/sprite/palette video composed per visible scanline,
  mid-frame palette-write coverage from the raster IRQ path, M72-style tile
  priority groups split around sprites, save-state, capability discovery, and
  real player screenshot/save-state smoke through
  `--system irem_m82`. `MNEMOS_M82_SET_DIR` is the real-artifact gate for these
  manifests and player smokes, including single-inner-set wrapper ZIP unwrapping
  and CRC-clean parent fallback.
  Remaining M82 work is palette banking, exact raster phase/timing, manual-backed
  DIP proof, and real visual-priority parity; it deliberately does not route
  R-Type II through the M72 board profile.

## Verification

- Every increment green-builds and green-tests (`ctest`) before commit;
  clang-format clean.
- Existing console suites untouched at every step (no shared code changes
  except the named additive extensions, each landing with its own tests).
- Data-gated suites: V30 singlestep corpus, R-Type boot/attract parity,
  protected/vertical M72 set probes, and the full checked-in true-M72 roster
  matrix (`MNEMOS_M72_SET_DIR`) plus `scripts/irem_m72/run-corpus-smoke.ps1`
  for player save/load/screenshot corpus smoke evidence; M82 R-Type II
  manifest/player coverage uses `MNEMOS_M82_SET_DIR`. The local Windows Irem
  corpus can be wired with `scripts\irem\run-local-corpus.ps1`; pass
  `-IncludeFullM72Roster` only when the strict M72 roster artifacts are complete.
